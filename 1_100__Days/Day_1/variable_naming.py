# Make the code readable by giving the Variable meaningfull name
# eg:-

user_name = "Vishal"
length = len(user_name)

# If you get the NameError in the terminal that means that some where in the code you misspelled the name.