def is_prime(num):
    for i in range(num + 1):
        if num%1 == 0 and num%num == 0:
            print(f"{num} is a prime number")
        else:
            print(f"{num} is not a prime number")
            

number = int(input("Enter the to check if it's prime of not: "))

is_prime(number)